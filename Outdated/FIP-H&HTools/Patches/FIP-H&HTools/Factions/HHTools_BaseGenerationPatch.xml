<?xml version="1.0" encoding="utf-8"?>
<Patch>
  <!-- Base Generation is currently shipping without the KCSG.CustomGenOption type this compat file expects. Keep the file as a no-op so the mod loads cleanly instead of hard-failing on missing XML classes. -->
</Patch>